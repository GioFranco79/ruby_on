<div class="container">
  <%= form_with(model: profesional) do |form| %>
    <% if profesional.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(profesional.errors.count, "error") %> prohibited this profesional from being saved:</h2>

        <ul>
          <% profesional.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :rut %>
      <%= form.text_field :rut %>
    </div>

    <div class="field">
      <%= form.label :nombre %>
      <%= form.text_field :nombre %>
    </div>

    <div class="field">
      <%= form.label :apellido %>
      <%= form.text_field :apellido %>
    </div>

    <div class="field">
      <%= form.label :fono %>
      <%= form.number_field :fono %>
    </div>

    <div class="field">
      <%= form.label :email %>
      <%= form.text_field :email %>
    </div>

    <div class="field">
      <%= form.label :titulo %>
      <%= form.text_field :titulo %>
    </div>

    <div class="field">
      <%= form.label :especialidad %>
      <%= form.text_field :especialidad %>
    </div>

    <div class="field">
    <%= form.label :tipo %>
    <%= form.number_field :tipo %>
  </div>

    <div class="field">
      <%= form.label :fnac %>
      <%= form.date_select :fnac %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
</div>