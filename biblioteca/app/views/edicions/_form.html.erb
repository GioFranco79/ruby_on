<%= form_with(model: edicion) do |form| %>
  <% if edicion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(edicion.errors.count, "error") %> prohibited this edicion from being saved:</h2>

      <ul>
        <% edicion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fechad %>
    <%= form.date_select :fechad %>
  </div>

  <div class="field">
    <%= form.label :libro_id %>
    <%= form.text_field :libro_id %>
  </div>

  <div class="field">
    <%= form.label :autor_id %>
    <%= form.text_field :autor_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
