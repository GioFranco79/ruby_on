<%= form_with(model: libro) do |form| %>
  <% if libro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(libro.errors.count, "error") %> prohibited this libro from being saved:</h2>

      <ul>
        <% libro.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :editorial %>
    <%= form.text_field :editorial %>
  </div>

  <div class="field">
    <%= form.label :autor, "Autores" %><br />
    <%= collection_select :ediciones, :autor_id, Autor.all, :id, :nom_com, {:prompt => "Seleccione los autores", :selected => @idau.map}, {:multiple => true} %>
  </div>

  <div class="field">
    <%= form.label :año %>
    <%= form.number_field :anho %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
